name: _sleep_test

on:
  workflow_dispatch:

jobs:
  sleep:
    runs-on: ubuntu-latest
    steps:
      - name: 睡眠 8000 秒（测试是否存在 ~2h 运行上限）
        run: |
          echo "sleep_test start $(date -u)"
          sleep 8000
          echo "sleep_test done $(date -u)"
