# 2026-07-09 工作日志

## 指标信号页 + 靠谱指数页 第3轮优化（已部署上线）

上一轮（第2轮）任务已全部完成并部署。本轮 10 项需求全部完成并部署：

### 指标信号 SignalPage.vue（#583-#587，上轮已完成本轮确认）
- #583 修复 Vue3 `ref` 不渲染到 DOM 的根因 bug（gauge/pie/radar/bond 图表不绘制）→ 改用 Vue `ref` 变量。修复宏观策略仪表盘空(Req7)、资产配比饼图不画(Req8)、风格因子蛛网图不画(Req9)。
- #585 指标注释（?悬浮提示+统计基准小字）+ 空状态说明（信用景气/海外联动无数据原因）。
- #586 响应式（overflow-x:hidden + 1100px 平板堆叠）+ 标签页 localStorage 持久化（key `af_signal_active_tab`）。
- #587 周期筛选（周/月/季/年）+ 市场筛选（全市场/沪深/港股建设中/美股建设中）+ FED图随周期变窗宽。

### 本轮新增改动（#584, #588）+ 部署
- #584 App.vue 品牌区改为「靠谱指数工具 ALLFUND.CN」（PC 顶部 logo）。
- #588 FundRankPage.vue「更多筛选」改为弹窗（Teleport+mask+居中 modal）：
  - 弹窗内筛选只改本地状态，点「确认」才 loadData；点遮罩/✕/取消恢复快照（moreFilterSnapshot）。
  - 新增「基金规模」预设：全部/大于2亿/2-5亿/5-10亿/10-20亿/20-50亿/50-100亿/100亿以上/自定义（SCALE_PRESETS）。
  - 更多筛选入口显示已选条件数量徽标（activeMoreFilterCount）。
  - 删除原内联展开 more-filter-body 及死代码 setSC/setFlag/setSG/setCN/setDailyLimit/onScaleConfirm。

### 部署
- `npm run build` 通过（665 modules）。
- EdgeOne Pages 部署成功（Production, Deployment ID dpwtqcvrgmce）。
- GitHub API 推送成功（commit fe3e89c4）。
- 注意：build 在仓库根目录生成了 vite.config.js.timestamp-*.mjs 临时文件并被 push 脚本提交，建议后续加入 .gitignore。
