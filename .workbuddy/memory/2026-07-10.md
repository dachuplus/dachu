# 2026-07-10 工作日志

## 1、取消未完成任务 + 数据模型规则文档化（本轮延续上轮）
- 删除任务列表最后 8 个 pending（#598/#599/#600/#601/#602/#603/#638/#639），任务列表清零（506 completed）。
- 规则：靠谱指数页数据来自 `fund_scores`（评分独立，不依赖 `fund_quarterly_scores`）；`fund_combined` 评分基于 `fund_quarterly_scores`。已写入 `docs/data-model-rules.md`，并改造 `sync_fund_combined_scores.py` + `update-fund-data.yml`（新增 步骤3b 刷新 fund_quarterly_scores）。
- 用户确认：fund_combined 是下载/验证用，与网站靠谱指数不同属正常。

## 2、夜间 CI 连续失败根因修复（QDII 分类回归）
- 现象：07-08、07-09 夜间 CI 均 failed；本次手动查日志定位失败步骤 = 步骤3c2 promote_staging 校验：`✗ 分类 [QDII] staging 220 >= 95% 生产 395`。
- 根因：175 只 QDII 海外指数基金在 staging 导入时被错判为 `指数型`（import_via_rest.py step2g 仅当 t1_tt 含 'QDII'/'海外' 才保留 QDII，但这些基金 t1_tt='指数型-股票' 无标记 → 滑落为指数型），QDII 395→220 触发 promote 整批拒绝。
- 修复（源头）：`fetch_and_import_funds.py` 对 `ft='qdii'` 抓取到的基金强制 `t0='QDII'`（天天基金 ft 为权威一级分类），不被聚源/指数子类覆盖。
- 加固：`import_via_rest.py` step2g 当 `t1_tt` 为带 '-' 的详细类型时同步 `t1=t1_tt`（使指数基金 t1=天天基金 '指数型-股票'，与网站展示一致）。

## 3、028336 长盛同庆中证800指数C 分类修复
- 问题：抓到的是「股票型基金」，天天基金显示为「指数型-股票」。
- 根因：028336 的 `t1_tt` 在当日构建时为 NULL（历史构建版本差异），step2g 回退到 hspj 粗分类「股票型基金」。
- 修复：直接 UPDATE 生产库 `fund_scores`(t0='指数型',t1='指数型-股票') 与 `fund_combined`(同)；并全局对齐——所有 `t1_tt` 为详细类型的基金 t1 同步为天天基金 f[3]（指数基金 t1 由旧的「指数型股票基金」统一改为「指数型-股票」，0 只残留）。
- 代码已加固（见 #2 的 step2g 改动），下一个夜间 run 会自动正确分类 028336。

## 4、历史遗留 git 状态清理
- 工作树有一批「recover 脚本」误 staged 的删除 + 重构中 src 改动（共 134 个变更）。
- 已 `git checkout HEAD --` 恢复被误删的管线依赖脚本：fetch_currency_funds.py / fetch_fund_basic_info.py / fetch_fund_indices.py / fetch_fund_indices_em.py / fetch_etf_returns.py / fetch_jqr_indicators.py / fetch_index_eva.py / ensure_basic_info_columns.py / promote_staging.py，以及 .workbuddy 记忆文件与 public/robots.txt、sitemap.xml。
- 注：推送脚本 `allfund_push_via_api.py` 会跳过删除项、排除 .xlsx/exports 等产物，故 GitHub 端不会丢失文件；本次推送 commit 564b80f3。
- ⚠️ 工作树仍含一个前端重构（src 多文件 M + 若干组件 D：FundIndexPanel.vue/HelpTip.vue/cache.js/format.js/sw-industry-map.json 等）。本次改动均为后端/文档，未重新部署 EdgeOne 前端；下次前端部署前需先 `npm run build` 验证这些被删组件是否仍被引用（重构若未完成会构建失败）。

## 5、推送与触发今日更新
- 推送 commit 564b80f3（QDII 修复 + t1 对齐 + 规则文档 + 历史脚本恢复 + 前端重构 working-tree 改动）。
- 手动 `workflow_dispatch` 触发今日夜间 CI（run 29100224378，22:32 北京时间开始），验证 QDII 校验通过、数据刷新。

## 晚间修复：基金指数页白屏

### 问题
用户反馈智能组合→基金指数 tab 页面完全空白（无数据、无表格、底部报"页面出现了一点小问题"）。

### 根因
`FundIndexPanel.vue` 第 58 行 `import { ref, onMounted } from 'vue'` **遗漏了 `computed`**，但第 120/126 行使用了 `computed()` 定义 `headCols` 和 `displayRows` → 运行时 ReferenceError → 组件崩溃白屏。

### 修复
- 在 import 中补上 `computed`：`import { ref, computed, onMounted } from 'vue'`
- `vite build` → EdgeOne 部署成功 → GitHub 推送 commit `eddf32a4`
- 数据库 `fund_scores` 正常（20,829 条，7 大类齐全），问题纯为前端导入缺失
