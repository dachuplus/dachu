# 2026-07-10

## 小修：.gitignore 忽略 Vite 构建临时产物
- 在 `allfund/.gitignore` 增加 `vite.config.js.timestamp-*.mjs` 与 `*.timestamp-*.mjs`，避免 `npm run build` 生成的重命名占位文件被提交。
- 已删除本地 3 个 `vite.config.js.timestamp-*.mjs` 临时文件（它们从未被 track，不会污染远程）。
- 推送脚本 `allfund_push_via_api.py` 基于本地工作区全部改动直接提交到 GitHub API（跳过已删除文件、上传所有变更），本地 git HEAD 不前进，故 `git status` 持续显示大量改动属正常现象。
- 部署/构建相关改动走沙箱专用推送：`GITHUB_TOKEN=ghp_... python3 scripts/allfund_push_via_api.py "msg"`。
