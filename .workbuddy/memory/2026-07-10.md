# 2026-07-10

## 小修：.gitignore 忽略 Vite 构建临时产物
- 在 `allfund/.gitignore` 增加 `vite.config.js.timestamp-*.mjs` 与 `*.timestamp-*.mjs`，避免 `npm run build` 生成的重命名占位文件被提交。
- 已删除本地 3 个 `vite.config.js.timestamp-*.mjs` 临时文件（它们从未被 track，不会污染远程）。

## 靠谱指数页 FundRankPage.vue 改造
### #590 自定义指标 → 评分指标弹窗
- 删除旧 `weight-entry-row` + `weight-panel` 内联展开面板和 `showWeightPanel` ref。
- 新增 `.filter-actions-row` 容器，内含「更多筛选」+「评分指标」两个并列按钮。
- 「评分指标」按钮点击弹出 `score-indicator-modal`（Teleport 到 body），复用原有 weightItems/weightSum 滑块逻辑；点遮罩/✕ 取消恢复原值，点确认生效并关闭弹窗。
- 清理旧 CSS（`.weight-toggle` / `.weight-panel-header` 等）替换为 `.score-indicator-modal` + `.score-indicator-toggle`。

### #591 表格列标题重命名 + 重排序 + 居中
- 列标题：代码→基金代码、简称→基金简称、管理费率→管理费%（数据去掉%符号，改 fmtManageFee 不追加%）。
- 收益列：近1年→近1年收益、近2年→近2年收益、近3年→近3年收益、近5年→近5年收益。
- 近2年列从第3位移到近1年和近3年之间（r1y→r2y→r3y→r5y），表头与数据列同步重排。
- 评分列标签加"评分"后缀：1年/2年/3年/5年 → X年评分。
- 操作列标题改为「投票」。所有 th text-align: center 居中。
- 移动端卡片同步更新标签文字（管理费%、近X年收益）。

### 部署
- EdgeOne 部署成功（dpbx5tgnjijf）→ GitHub 推送完成（commit 59c43c56）。
