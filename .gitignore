node_modules/
dist/
dist.zip
.env
.env.local
.env.*.local
*.pyc
__pycache__/
.DS_Store
*.log
*.ndjson
# Vite 构建临时产物（node 重命名占位）
vite.config.js.timestamp-*.mjs
*.timestamp-*.mjs

# 导出/临时数据（本地保留，不入git）
exports/fund_combined.xlsx
exports/fund_combined_new_scores.xlsx
exports/fund_raw_sample_classification.xlsx
exports/fund_raw_sample_full.xlsx
exports/k_scores_backup_before_recompute.json
fund_raw_sample.xlsx
fund_raw_sample_full.xlsx
fund_raw_sample_scored.xlsx
# 数据下载中心的导出文件：不再放 public/（会被 EdgeOne 静态托管公开），
# 统一落在 exports/downloads/，由 scripts/upload_downloads.py 上传到 Supabase 私有桶。
# 注：exports/downloads/index.json 是清单，需要入库跟踪；xlsx 体积大不入 git。

# 本地工具态（不入git）
.workbuddy/automations/
.workbuddy/memory/2026-07-11.md

# 自动导出的数据文件（CI 生成，不入git）
exports/downloads/*.xlsx

# Supabase CLI 运行时临时产物（自动生成，不入git）
supabase/.temp/

# 服务端密钥文件（仅 EdgeOne Function 运行时使用，绝不进 git）
functions/api/_deepseek_key.js
