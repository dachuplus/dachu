# ALLFUND.CN 最高风控规则（数据真实性铁律）

> 本文件为 ALLFUND.CN 项目的**最高优先级风控规则**，凌驾于一切便利性、兜底、美观需求之上。
> 任何前端代码、后端 ETL 脚本、数据库写入逻辑，都必须遵守本规则。违反本规则视为严重事故。

---

## 一、总则（最高原则）

**金融行业不做假数据。查不到就显示 `--`（或留空），坚决不能用假数据兜底。**

1. 真实、可追溯的数据 > 看起来好看的数据。
2. 当某个字段/指标**没有真实数据**时，前端统一显示 `--`，后端统一留空（NULL），**绝不**用硬编码、均值、上一期、占位数字等任何形式"凑数"。
3. "兜底" = "造假" 在这里是同义词。任何形式的兜底数据（硬编码列表、预设收益率、模拟值）都禁止出现。

---

## 二、前端规则

1. **无数据显示 `--`**：所有指标、收益、排名、时间等字段，凡 `null / undefined / 空` 一律渲染为 `--`（或按页面约定显示 `—` / `暂无`）。不得出现任何预设的示例数字。
2. **禁止 mock / 模拟值展示**：不得存在"当前为模拟值""示例数据""占位展示"之类的展示逻辑（如 `jqrIsMock` 类提示）。如真实数据暂未同步，直接显示 `--`，不得用假数据填充界面。
3. **数据来源统一显示 `ALLFUND.CN`**：
   - 所有第三方抓取来源（天天基金、蛋卷、腾讯行情、value500、akshare、qt.gtimg 等）在页面上**统一归因展示为 `ALLFUND.CN`**。
   - 仅**官方公开机构**可保留原名：国家统计局、中国人民银行、美国国债（U.S. Treasury）、中债（ChinaBond）。
   - 抓取类来源即使页面内部有技术注释，对外展示也只写 `ALLFUND.CN`。

---

## 三、后端 / ETL 规则

1. **禁止硬编码兜底数据**：不得在脚本中内置标签列表、收益率、净值等"BUILTIN / 默认值"数据并在拉取失败时回退写入。
   - 例：`fetch_fund_tags.py` 的 `BUILTIN_TAGS`（131 条手写标签 + 写死收益率）属于**违规遗留代码**，已弃用且不在 CI 流水线中，须彻底删除，不得复活。
2. **真实 API 拉取失败 = 留空 / 跳过，不回退**：
   - 当上游接口（ZTJJ、push2、akshare、value500 等）返回空或超时，**直接跳过该批次写入**，绝不用内置假数据补位。
   - 已有真实数据的行保持不动；缺失的行保持 NULL。
3. **Staging 管道强制校验**（每日自动更新）：`import_via_rest` 写入 `fund_scores_staging` 后，必须经 `promote_staging.py` 严格校验（总量 ≥19000、各大类 ≥95%、货币型 ≥900 且 k_all 非空 ≥90%、r1y 非空 ≥90%）+ 备份 + 原子切换，失败拒绝并保留生产，**不得清空生产表**。
4. **任何新 ETL 脚本不得新增硬编码兜底**：如需默认值，只允许"跳过/留空"语义，不允许"写入预设数字"。

---

## 四、数据来源展示规范（对外）

| 场景 | 对外显示 |
|------|----------|
| 第三方抓取（天天基金 / 蛋卷 / 腾讯 / value500 / akshare / qt.gtimg 等） | `数据来源：ALLFUND.CN` |
| 国家统计局 | 国家统计局（保留） |
| 中国人民银行 | 中国人民银行（保留） |
| 美国国债 | 美国国债收益率（保留） |
| 中债国债收益率曲线 | 中债国债收益率曲线（ChinaBond，保留） |

---

## 五、已知技术债（须整改，不得新增）

- [ ] `scripts/fetch_fund_tags.py`：`BUILTIN_TAGS` 硬编码标签 + 写死 `return_pct`，违规。**已弃用、不在 CI，须删除**。
- [ ] `scripts/setup_fund_tags.py`：`FORCE_BUILTIN` 开关，违规。**已弃用，须删除**。
- [ ] 前端 `SignalPage.vue` 的 `jqrIsMock` 模拟值提示：违反"禁止 mock 展示"原则，后续须改为 `--`。

---

## 六、违规处理

- 任何提交（PR / 直接部署）若引入硬编码兜底数据、mock 展示、或对外暴露抓取源，视为**阻断级问题**，须回滚并重写。
- 代码审查须检查：是否存在 `BUILTIN*`、`*_FALLBACK`、`硬编码数字兜底`、`mock/模拟` 等模式。
- 本规则优先于"先上线再说""数据好看""兼容旧逻辑"等任何理由。

---

*最后更新：2026-07-11*
