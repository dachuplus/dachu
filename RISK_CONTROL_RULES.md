# 项目最高风控规则（ALLFUND / dachu）

本文件定义 dachu 项目的最高优先级风控规则。任何开发、部署、推送操作都必须先满足以下规则，优先级高于一切功能需求。

## 规则 1（最高优先级）：本地文件必须与 GitHub 云端逐字节一致

- 所有提交到仓库的文件，本地工作区版本必须与 `origin/main` 上的版本完全相同（逐字节一致）。
- 禁止出现「本地有而云端没有」或「云端有而本地没有」的差异文件（除非该文件明确在 .gitignore 中）。
- 每次推送后，必须执行 `git fetch origin main && git reset --soft origin/main`（或等价对齐动作），确认本地 HEAD 与云端一致。
- 禁止用 `git reset --hard` 清除未提交的有效改动；如必须清理，先与云端对齐并确认。
- 推送只允许通过受控脚本（如 push_targeted.py，基于 blob→tree→commit→update_ref）精准提交指定文件，不碰无关的 messy index。
- 任何脏文件（staged 删除 / untracked 副本）必须先列出给用户确认，确认后再处理，禁止擅自删除。

## 规则 2：锁定期/定开产品剔除（持仓安全）

- 选基因组合时，必须剔除名称含「持有期 / 定开 / 定期开放 / 最短持有 / N年或月持有」等锁定期产品（按名称正则，fund_scores 无专用列）。
- 月度调仓产品必须可随时赎回，锁定期内卖不掉的产品一律排除。

## 规则 3：数据源与模型幻觉防控

- AI 大PK 选基严格基于 fund_scores 真实表数据，禁止引用基金经理、任职年限、公司历史等任何表外/网络信息（视为臆测、判无效）。
- 给模型提问时必须明确「当前年份锚点（今天 2026 年）」与「返回字数上限」，避免模型外推未提供年份（如 2024E）或返回过长导致超时/截断。
- 前端调仓时间线只展示模型返回的真实文本，不做二次加工或编造。

## 规则 4：任何数据必须展示来源与时间（数据可追溯）

- 前端展示的任何数据（列表、卡片、弹窗、分享图、图表等），只要呈现具体数值，都必须同时展示「数据来源」与「数据截止/更新时间」。
- 数据来源统一对外展示为 `大厨先生`（官方公开机构除外，如国家统计局/央行/美国国债/中债，可保留原名）。
- 截止时间优先取该数据对应的真实更新时间（如 fund_scores_meta 的 tsq / update_time / nav_date），查不到显示 `—`，禁止编造时间。
- 典型落地位置：热门标签弹窗、分享弹窗/分享图、靠谱指数底部、指标信号各图表、数据下载中心等。新增任何展示数据的组件都必须遵守本规则。

## 规则 5：数据库所有表必须进入数据下载中心（可下载验证）

- Supabase 数据库中的**所有** public 表都必须出现在数据下载中心（DataCenterPage），供授权账户下载 Excel 验证。
- **后续新建的任何表都必须自动纳入**：导出脚本 `scripts/export_all_tables.py` 通过 Supabase Management API 自动发现全部 public 表并导出，前端 `DataCenterPage.vue` 依据导出产物 `index.json` 动态渲染表清单——新建表无需改代码即自动出现。
- 若因权限等原因自动发现失败，回退到内置表清单；内置清单必须与数据库实际表保持同步，发现遗漏须及时补齐。
- 用户数据表（user_portfolios / user_profiles 等敏感表）仅对授权账户开放下载。

## 规则 6：所有「提取基金」查询一律查 fund_scores（fund_combined 仅用于下载验证）

- dachu 前端/运行期凡是需要「提取/检索基金产品」的场景（组合生成、基金详情、榜单、筛选、AI 选基等），**只能查询 `fund_scores` 表**，不得查询 `fund_combined`。
- `fund_scores` 含完整字段（`c` 带 `.OF` 后缀、`n` 名称、`t0`/`t1`/`t1_tt` 分类、`company` 公司、`fund_scale` 规模、各周期收益/回撤/夏普、`k_all`/`score_grade` 评分等），足以支撑所有取数需求。
- `fund_combined` 是**下游派生/校验表**（由 `fund_scores` 同步分类、补充详情、重算评分后生成），定位为「下载验证用」——只在数据下载中心展示、导出 Excel 供人工核对，不参与任何运行期取数。
- 历史坑：曾误用 `supabase.from('fund_combined')` 取 AI 组合候选池，但 `fund_combined` 无 `t1_tt` 列（该列只在 `fund_scores`），导致 `.eq('t1_tt', cat)` 返回空集、组合无法生成。凡取数一律走 `fund_scores` 即可避免此类列名错配。
- 后端 ETL/同步脚本（sync_fund_combined_scores、create_fund_combined、各类 fix_*、export_*）对 `fund_combined` 的读写属于「维护该派生表本身」，不在本规则禁止范围内，但其产出的下游数据最终仍须源自 `fund_scores`。
